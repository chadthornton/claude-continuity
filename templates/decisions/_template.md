# Feature Name

## Decided
<!-- Each item needs a *why*. Optionally prefix a stable handle (D1, D2, …) so
     an Open item, a Ruled out entry, or another note can reference it: "(see D2)".
     Handles are within-file only and retire when the decision is pruned — this
     is not an append-only ADR log. -->
(none yet)

## Open
<!-- Optionally prefix items to distinguish kinds:
     [decision] a question whose answer is a choice — resolve before building
     [task] queued work, nothing to decide
     Unmarked items are fine and render as before. -->
(none yet)

## Ruled out
<!-- Optional. Approaches tried and abandoned, one line each:
     `approach — why it failed`. Keeps the next session from re-walking a dead
     end (distinct from Open, which is undecided, and blind_spots, which are
     session gotchas). Prune an entry once the code path that would hit it is
     gone. Omit this section entirely if empty. -->

## Not yet specified
<!-- Optional. Decisions you can see coming but can't phrase precisely yet
     (fog of war). Distinct from feature-status blind_spots (backward-looking
     gotchas). Park the dim view here; it graduates into an Open item once sharp.
     Omit this section entirely if empty. -->
